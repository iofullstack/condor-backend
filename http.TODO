Documentación de las peticiones http:

  ## CLIENT

  POST:
    /api/clients
      @send
        {
          nit_passport,
          firstName,
          lastName
        }
      @return
        {
          message
        }